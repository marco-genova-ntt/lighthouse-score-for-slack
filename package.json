{
  "name": "lighthouse-score-for-slack",
  "version": "1.0.0",
  "description": "LightHouse Score Publisher - Module used to communicate with slack",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node --require 'dotenv/config' --require 'babel-register' dist/app/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marco-genova-ntt/lighthouse-score-for-slack"
  },
  "keywords": [
    "slack",
    "google lighthouse"
  ],
  "author": "marco.genova@nttdata.com",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "ramda": "^0.26.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^24.0.0",
    "rimraf": "^2.6.3"
  }
}
